services:
  nginx:
    build:
      context: nginx
      dockerfile: dockerfile
    image: public.ecr.aws/w3l0w0b0/peter_tester
    container_name: nginx

  php-fpm:
    build:
      context: php
      dockerfile: dockerfile
    image: public.ecr.aws/w3l0w0b0/php-fpm
    container_name: php-fpm
